{
  "name": "skycast-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://cloneofghosts.github.io/skycast",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "lucide-react": "^latest_version",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "tailwindcss": "^latest_version",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "gh-pages": "^latest_version",
    "autoprefixer": "^latest_version",
    "postcss": "^latest_version"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
